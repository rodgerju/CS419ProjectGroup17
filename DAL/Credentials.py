class Credentials:

	def __init__(self, name, pwd, host, port, dbname, dbtype):
		self.username = name
		self.password = pwd
		self.host = host
		self.port = port		
		self.dbname = dbname
		self.dbtype = dbtype
		
